SELECT *
FROM posts
ORDER BY created_at DESC;
-- The home.html page displays the most recent posts from all topic pages (food.html , events.html , culture.html , places.html ).
-- This query does not update, insert, or delete anything in the database.
-- It only sorts all of the posts, displaying most recent posts at the top.
